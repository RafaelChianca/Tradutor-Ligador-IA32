add dword eax;03 05

sub dword eax;2b 05

imul dword;f7 2d

idiv dword;f7 3d

jmp dword;e9

cmp dword eax, 0; 83 f8 00

jl EscreverChar;7c

jg LeerString;0f 8f

je EscreverString;0f 84

mov dword ebx, [B];8b 1d

mov dword [B], ebx

mov dword eax, [B]

mov dword [B], eax

push dword eax;

push dword B

call LeerInteiro

pop dword eax

call EscreverInteiro

call LeerChar

mov dword ebx, esp

sub dword ebx, 4

call EscreverChar

push B

call EscreverHexa

call LeerString

call EscreverString

mov eax, 1

mov ebx, [B]

int 80h


jne LeerHexa

LeerChar:
add dword eax, [B]

sub dword eax, [B]

EscreverChar:
add dword eax, [B]

sub dword eax, [B]

LeerInteiro:
add dword eax, [B]

sub dword eax, [B]

EscreverInteiro:
add dword eax, [B]

sub dword eax, [B]

LeerString:
add dword eax, [B]

sub dword eax, [B]

EscreverString:
add dword eax, [B]

sub dword eax, [B]

LeerHexa:
add dword eax, [B]

sub dword eax, [B]

EscreverHexa:
add dword eax, [B]

sub dword eax, [B]
